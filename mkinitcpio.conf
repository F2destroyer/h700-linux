# mkinitcpio configuration for H700 SoC
# This is a minimal configuration for creating an initramfs

MODULES=()
BINARIES=()
FILES=()
HOOKS=(base udev autodetect modconf block filesystems keyboard fsck)
